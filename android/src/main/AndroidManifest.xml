<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <meta-data
            android:name="com.webengage.child.expo"
            android:value="rn_expo:${we_expo_version}" />
    </application>
</manifest>
